# Runtime dependencies for the normativ.kontur.ru search tool.
requests>=2.31.0
beautifulsoup4>=4.12.0
urllib3>=2.0.0

# Used by the shared html_legal_text_parser when text extraction needs a DOM snapshot.
playwright>=1.40.0
